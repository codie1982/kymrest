/*   * To change this license header, choose License Headers in Project Properties.  * To change this template file, choose Tools | Templates  * and open the template in the editor.  */var job_settings_form = function () {    var job_settings_form = function () {        let options = {            component_name: "job_settings_form",            secret_number: false,            formGroup: true,            templateStyle: [                {                    parameter: "background",                    value: "#fff",                }            ],            content: [],            form_complete: function () {                product_settings_extra_price.init();            }        };        var predata = document.querySelectorAll('[data-' + options.component_name + ']');        if (predata.length !== 0) {            for (let i = 0; i < predata.length; i++) {                options.content = (JSON.parse(atob(predata[i].getAttribute("data-json"))));            }        } else {            var predata = document.querySelector('[reset-' + options.component_name + ']');            options.content = (JSON.parse(atob(predata.getAttribute("data-json"))));        }        $(".job_setings_form_content").makeForm(options);    }    var tag_input = () => {        var customer_tag = $("#atthedoor_options")        customer_tag.tagsinput();    }    return {        init: function () {            job_settings_form();            tag_input();        }    }}();jQuery(document).ready(function () {    job_settings_form.init(); // init metronic core componets});